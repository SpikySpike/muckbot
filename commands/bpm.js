module.exports = {
    name: 'bpm',
    description: 'get the song bpm',
    async execute(message, args, Discord) {
        /*
        const Audio = File
        const attachIsAudio = (message.attachments === Audio)

        if (message.attachments.size > 0) {
            if (message.attachments.every(attachIsAudio)){
                message.reply('hi')
            }
            else {
                message.reply('pls mp3')
            }
        }
        else {
            message.reply('no')
        }*/
        message.reply('Command in progress!')
    }
}